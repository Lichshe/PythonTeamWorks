将程序放入某个磁盘中，运行ScanQiusbk.py这个程序，程序会提示你选择运行方式：
“O”代表单步运行，通过点击回车键，输出一条糗事百科内容，不进行保存。
“T”代表一次性搜索一个页面并将页面中的内容保存在本地的磁盘中，
程序会在程序所在的目录下建立一个文件夹，文件夹以每次运行时的时间命名，
得到的文本信息保存在在result.txt文本文件中，对每条糗事百科进行编号
得到的图片保存在和文本信息同样的目录下，并以相应糗事百科的编号进行命名，
在每次一次性搜索时，会输出当前页面中最好笑的糗事百科。
程序运行的任何时间都可以通过输入“Q”使程序终止。

